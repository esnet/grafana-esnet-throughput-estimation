export interface SimpleOptions {
  labelx: string;
  labely: string;
  scalex: string;
  scaley: string;
  defaultTimeValue: number;
  defaultTimeInterval: string;
  tickFormatX: string;
  tickFormatY: string;
  algorithm: number;
  boundary: number;
  boundaryUnits: number;
}
